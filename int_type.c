#include <stdio.h>

int main(void)
{
    // Declare char type parameter ch1 and initiate it as 'A' at the same time
    char ch1 = 'A';
    
    // Declare char type parameter ch2 and initiate it as 65 at the same time
    char ch2 = 65;
  
  printf("The ASCII code value of character %d: %d\n", ch1, ch1);
	printf("The character that has ASCII code value %c: %c\n", ch2, ch2);

	return 0;
}
